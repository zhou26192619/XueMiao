<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:orientation="vertical">

    <cc.xuemiao.ui.view.HeadView
        android:id="@+id/headView"
        android:layout_width="match_parent"
        android:layout_height="@dimen/head_h"
        android:background="@color/head_bg" />

    <com.handmark.pulltorefresh.library.PullToRefreshListView
        android:id="@+id/cooperation_detail_iv_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/headView"
        android:divider="@drawable/rectangle_organization_divider"
        android:listSelector="@android:color/transparent" />

    <ImageView
        android:id="@+id/cooperation_detail_iv_reply"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="@dimen/comment_padding_right"
        android:layout_marginRight="@dimen/comment_padding_right" />

</RelativeLayout>