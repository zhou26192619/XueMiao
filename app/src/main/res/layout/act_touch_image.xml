<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    android:orientation="vertical" >

    <com.lib_common.widgt.HackyViewPager
        android:id="@+id/touch_image_vp_images"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="#40000000"
        android:orientation="horizontal"
        android:paddingBottom="10dp"
        android:paddingLeft="@dimen/comment_padding_left"
        android:paddingRight="@dimen/comment_padding_right"
        android:paddingTop="10dp" >

        <ImageView
            android:id="@+id/touch_image_iv_delete"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:src="@mipmap/delete"
            android:visibility="gone" />

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_weight="1" />

        <ImageView
            android:id="@+id/touch_image_iv_save"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:src="@mipmap/save_text"
            android:visibility="gone" />
    </LinearLayout>

</RelativeLayout>